import { Request, Response } from "express";

export default async function(req: Request, res: Response) {
  // do some data validations
  // upload the file to s3
  // save the file record to our db with the important info
};
